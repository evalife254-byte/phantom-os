# Phantom OS — Journal optimization
# Reduce journal overhead for better performance

[Journal]
# Limit journal size
SystemMaxUse=100M
RuntimeMaxUse=50M

# Reduce sync frequency
SyncIntervalSec=60s

# Forward to syslog disabled (reduces overhead)
ForwardToSyslog=no
