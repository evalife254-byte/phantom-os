# Phantom OS — tmpfiles configuration for performance

# Create phantom config directory
d /etc/phantom 0755 root root -

# Optimize scheduler for desktop responsiveness
w /sys/kernel/mm/transparent_hugepage/enabled - - - - madvise
w /sys/kernel/mm/transparent_hugepage/shmem_enabled - - - - advise
